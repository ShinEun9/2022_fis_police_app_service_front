import React from 'react';
import {Text, SafeAreaView} from "react-native";

function CheckReservationTemplate(props) {
    return (
        <SafeAreaView>
            <Text>CheckReservationTemplate</Text>
        </SafeAreaView>
    );
}

export default CheckReservationTemplate;